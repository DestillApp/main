/**
 * @interface LoginForm
 * @description Interface representing the structure of the login form data.
 * @property {string} email - The user's email address.
 * @property {string} password - The user's password.
 */
interface LoginForm {
  email: string;
  password: string;
}
